---
apiVersion: v1
kind: Secret
metadata:
  name: argocd-repository-${name}
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: ${type}
type: Opaque
stringData:
%{~ if type == "repository" }
  type: git
%{~ endif }
  url: ${url}
%{~ if username != null }
  username: ${username}
%{~ endif }
%{~ if password != null }
  password: ${password}
%{~ endif }
%{~ if ssh_private_key != null }
  insecure: "true"
  sshPrivateKey: |
    ${replace(replace(ssh_private_key, "\r\n", "\n"), "\n", "\n    ")}
%{~ endif }
%{~ if secret != null }
  password: ${secret}
%{~ endif }
%{~ if github_app_id != null }
  githubAppID: "${github_app_id}"
%{~ endif }
%{~ if github_app_installation_id != null }
  githubAppInstallationID: "${github_app_installation_id}"
%{~ endif }
%{~ if github_app_private_key != null }
  githubAppPrivateKey: |
    ${replace(replace(github_app_private_key, "\r\n", "\n"), "\n", "\n    ")}
%{~ endif }
